import 'package:flutter/material.dart';

class ActivityJoinedPage extends StatelessWidget {
  const ActivityJoinedPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Activity Joined Page'),
      ),
    );
  }
}
